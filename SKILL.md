---
name: flowspec
description: 将模糊的软件产品需求、PRD、会议纪要、问题复现步骤或现有流程图生成、重构或审计为可评审的业务流程与状态流转规格；区分事实、假设和信息缺口，直接输出可由 draw.io 打开编辑的原生 .drawio 图文件，检查异常路径、分支、循环与状态冲突，并把需求来源、节点/边、验收标准和测试场景建立追踪。用于需求澄清、产品流程图、状态机、流程审计和变更影响分析；不用于只做 draw.io/Mermaid 美化、系统架构图、组织流程/BPMN 全套咨询或通用数据可视化。
metadata:
  author: Li
  version: "0.5.0"
---

# FlowSpec

把流程图当作需求验证与追踪工具，而不是把原文逐句换成节点。结果应让产品、开发和测试看见边界、证据、异常路径与可执行验收。

## Router Rules

- **新建/重构**：从需求、PRD、纪要、反馈或复现步骤生成流程规格。
- **审计**：检查现有 draw.io、Mermaid 或其他流程图的逻辑、异常分支、状态与可测试性；没有修改授权时只报告，不改文件或规则。
- **状态流转**：核心是实体生命周期、事件、守卫条件或非法转换时使用 `diagram_type: state`；否则默认 `diagram_type: flowchart`。两者都生成原生 `.drawio`。
- **变更影响**：比较前后版本的稳定 ID，指出受影响的来源、节点/边、假设、验收、测试与可观测字段。

不要触发于：只美化 draw.io/Mermaid 或修复 Mermaid 语法、系统架构/时序/ER 图、一般数据可视化、完整 BRD/PRD 写作、企业 BPMN 咨询。用户显式调用时仍说明能力边界。

## Gate Ladder

1. **快速梳理**：低风险、一次性讨论或用户只要草图时，输出目标与边界、事实/假设/关键缺口、可编辑 `.drawio` 流程图、最高优先级问题和 3–7 条验收标准。
2. **完整评审**：默认用于非简单软件流程；增加逻辑审计、完整测试场景和追踪矩阵。
3. **机器规格**：用户要求保存、反复迭代、版本比较或确定性校验时，生成规范化 JSON 并运行脚本。

不要为了套模板而输出无关章节。安全、隐私、资金、权限、持久化数据、迁移、外部写操作或跨系统状态存在时，不得降为快速梳理。

## 证据与状态纪律

1. 先读取用户提供的原文、文件、截图、接口、日志或现有图；能从现有材料核实的技术事实不反问用户。
2. 严格分开：
   - **已确认事实**：指向需求原文、用户确认、接口契约、日志或现状证据；
   - **待验证假设**：闭合草案所需但尚未证明，写明风险与验证方式；
   - **信息缺口**：用问题表达，标注 owner 与是否阻塞；
   - **主观建议**：说明取舍依据，不伪装成业务规则。
3. 不凭常识发明阈值、权限、超时、重试次数、状态保留、第三方能力或业务结果。
4. 只立即追问会改变安全/合规/资金/权限/数据结果、不可逆动作、对外契约或主路径的阻塞问题。其余缺口留在草案中继续推进。
5. 状态必须诚实：
   - `draft`：允许显式阻塞问题与未验证假设；
   - `review_ready`：没有开放的阻塞问题，并完成结构校验与语义检查；
   - `verified`：只用于已有真实证据的范围，且不含未验证假设。
6. 分开报告静态校验、测试/模拟环境、真机、人工评审与生产观测。draw.io 可打开、脚本通过或逻辑自洽都不等于业务规则真实。

## Compact Workflow

1. 明确目标、参与角色、系统边界、入口、成功终点、失败/取消终点，以及范围内外。
2. 建立节点和边；关键节点至少记录责任角色、前置条件、输入/输出和需求来源。关键分支需要来源，必要时记录日志/埋点。
3. 只按场景与风险检查相关失败模式：校验失败、权限拒绝、业务规则拒绝、依赖失败、网络中断、超时、重复提交/幂等、取消/返回、重试上限、并发/陈旧状态、恢复与人工兜底。
4. 检查结构不变量：
   - 一个显式开始，至少一个明确终点；
   - 所有节点从开始可达，所有非终点最终可到达终点；
   - 决策是可判定问题，分支互斥且共同完备，或有明确默认分支；
   - 循环有退出条件，重试有上限或停止策略；
   - 外部写操作考虑超时后的未知结果、幂等、查询或对账；
   - 状态转换包含事件、守卫、目标状态和副作用，并识别高风险非法转换。
5. 使用 ASCII 稳定节点 ID；原生 draw.io 节点和边保留 FlowSpec ID、来源、假设与验收元数据。仅当责任边界影响理解时使用泳道。
6. 为关键节点或关键边生成可观察的 Given/When/Then 验收标准，补充不得发生的副作用与验证方式；再生成覆盖正常、边界、错误、状态和恢复路径的测试。
7. 建立追踪：`需求来源 -> 节点/边 -> 验收标准 -> 测试场景`。没有来源或尚未确认的内容必须暴露，不能静默补齐。

## Output Contract

完整评审默认按以下顺序；快速梳理可按前述规则裁剪：

1. 状态、范围与流程目标
2. 已确认事实、待验证假设、阻塞问题、非阻塞问题
3. 可编辑流程图或状态图：采用原生 `.drawio` 文件
4. 逻辑审计结果：严重度、问题、证据、影响、建议
5. 验收标准：稳定 ID、关联节点/边、Given/When/Then、不得发生、验证方式
6. 测试场景：完整路径、前置、步骤、预期、结束状态、可观测证据
7. 追踪矩阵
8. 变更影响（仅在有基线时）

用户指定格式时保留同等信息。无法证明的结论必须标成“逻辑推导”或“需真实环境/接口/设备验证”。

## 机器可校验产物

进入机器规格模式前读取 [references/model-contract.md](references/model-contract.md)，保存规范化 JSON 后运行：

```bash
python3 scripts/flowspec.py validate <spec.json>
python3 scripts/flowspec.py validate <spec.json> --strict
python3 scripts/flowspec.py render <spec.json>
python3 scripts/flowspec.py render <spec.json> --output <flow.drawio>
python3 scripts/flowspec.py render <spec.json> --output <flow.drawio> --force
python3 scripts/flowspec.py render <spec.json> --format markdown --output <flowspec.md>
python3 scripts/flowspec.py diff <before.json> <after.json> --format markdown --output <impact.md>
```

`render` 默认在输入 JSON 旁生成同名 `.drawio`；显式输出路径也必须以 `.drawio` 结尾。已有文件默认保留，确认替换时使用 `--force`。图文件是未压缩的原生 XML，节点、边、泳道和追踪元数据可在 draw.io 中继续编辑；`markdown` 用于文本评审。普通 `validate` 允许带警告的 `draft`；`--strict` 用于 Review-ready 候选。脚本只证明结构不变量、直接引用与可重复渲染，不能证明业务语义、平台能力或真实环境行为。

定稿或审计时再读取 [references/review-checklist.md](references/review-checklist.md) 做人工语义检查。完整结构样例见 [references/login-security.example.json](references/login-security.example.json)，其中业务阈值仍是待验证假设，不得复用为真实策略。
